{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "globalEnv": [
    "NODE_ENV",
    "PORT",
    "DATABASE_URL",
    "TELEGRAM_BOT_TOKEN",
    "SOLANA_RPC_URL",
    "SOLANA_NETWORK",
    "METEORA_API_URL",
    "PRIVY_APP_ID",
    "PRIVY_APP_SECRET",
    "PRIVY_AUTH_ID",
    "PRIVY_AUTH_PRIVATE_KEY"
  ],
  "tasks": {
    "build": {
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env*",
        "src/**/*",
        "tsconfig.json",
        "tsup.config.ts"
      ],
      "outputs": [
        "dist/**",
        ".vercel/**",
        "build/**",
        ".next/**",
        "!.next/cache/**"
      ],
      "cache": true
    },
    "build:bot": {
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env*",
        "src/**/*",
        "tsconfig.json",
        "tsup.config.ts"
      ],
      "dependsOn": ["^build"],
      "outputs": [
        "dist/**",
        ".vercel/**",
        "build/**",
        ".next/**",
        "!.next/cache/**"
      ],
      "cache": true
    },
    "test": {
      "inputs": [
        "src/**/*",
        "**/*.test.*",
        "**/*.spec.*",
        "jest.config.*",
        "vitest.config.*"
      ],
      "outputs": ["coverage/**"],
      "dependsOn": ["^build"],
      "cache": true
    },
    "lint": {
      "inputs": [
        "src/**/*",
        "eslint.config.*",
        ".eslintrc.*"
      ],
      "dependsOn": ["^build"],
      "cache": true
    },
    "check-types": {
      "inputs": [
        "src/**/*",
        "tsconfig.json"
      ],
      "dependsOn": ["^build"],
      "cache": true
    },
    "dev": {
      "dependsOn": ["^build"],
      "cache": false,
      "persistent": true
    }
  }
}
